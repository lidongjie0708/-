<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuyuan.thumb.mapper.ThumbMapper">

    <resultMap id="BaseResultMap" type="com.yuyuan.thumb.model.entity.Thumb">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="userId" column="user_id" jdbcType="BIGINT"/>
            <result property="blogId" column="blog_id" jdbcType="BIGINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,blog_id,
        create_time
    </sql>

    <insert id="insertIgnore">
        INSERT IGNORE INTO thumb (id, user_id, blog_id)
        VALUES (#{thumb.id}, #{thumb.userId}, #{thumb.blogId})
    </insert>

    <delete id="deleteByUserAndBlog">
        DELETE FROM thumb
        WHERE user_id = #{userId} AND blog_id = #{blogId}
    </delete>
</mapper>
